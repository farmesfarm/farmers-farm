{
  "name": "farmers-farm-root",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && node server.js",
    "install-deps": "cd backend && npm install"
  }
}
